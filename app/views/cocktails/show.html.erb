<h1><%= @cocktail.name %></h1>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li class="dose"><%= dose.description %> <%= dose.ingredient.name %> <%= link_to "🗑", dose_path(dose), :method => :delete, data: {:confirm => "You sure?" } %></li>
  <% end %>
</ul>
<%= link_to "+ Add an ingredient", new_cocktail_dose_path(@cocktail), class: "btn btn-primary" %>
